# ZombieWarWeaponList.json
### Fields
```
{
	"Version": <int>, // config version (def. 0)
	// Zombie War Weapon list
	"Weapons": [<int>] // weapon IDs (array) (def. empty)
}
```
def. means that if the field is undefined, the default value will be used.